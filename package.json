{
  "name": "syntra-pos",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Syntra POS - Sistema de Gestión",
  "scripts": {
    "lint": "eslint --ext .js,.mjs .",
    "format": "prettier --write \"**/*.{js,mjs,css,html,json,md}\"",
    "format:check": "prettier --check \"**/*.{js,mjs,css,html,json,md}\"",
    "test": "node --test"
  },
  "devDependencies": {
    "eslint": "^8.56.0",
    "prettier": "^3.2.0"
  }
}
